{
  "name": "cari-kata",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "seed:test": "bun scripts/insert.ts",
    "test": "vitest",
    "test:fresh": "bun run seed:test && vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "hono": "^4.11.7"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.7",
    "@types/bun": "^1.3.8",
    "@vitest/coverage-istanbul": "4.1.4",
    "miniflare": "^4.20260415.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.83.0"
  }
}
